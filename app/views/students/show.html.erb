<p id="notice"><%= notice %></p>

<% if @student.photo.present? %>
  <%= image_tag(@student.photo_url) %>
<% end %>

<p>
  <strong>Given name:</strong>
  <%= @student.given_name %>
</p>

<p>
  <strong>Family name:</strong>
  <%= @student.family_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @student.email %>
</p>

<p>
  <strong>Team:</strong>
  <%= link_to @student.team.name, team_path(@student.team_id) %>
</p>

<p>
  <strong>Qualificação:</strong>
  <% if @student.admin %>
    Administrador
  <% else %>
    Membro
  <% end %>

</p>

Empréstimos aguardando aprovação:
<%= render partial: 'loans', locals: {student: @student, loans: @pending_loans, pending: true, approved: false, rate: false} %>

Empréstimos pedidos:
<%= render partial: 'loans', locals: {student: @student, loans: @student_asked_loans, pending: false, approved: false, rate: false} %>

Empréstimos concedidos em aberto:
<%= render partial: 'loans', locals: {student: @student, loans: @student_approved_loans, pending: false, approved: true, rate: false} %>

Empréstimos concedidos finalizados:
<%= render partial: 'loans', locals: {student: @student, loans: @finished_loans, pending: false, approved: false, rate: true} %>

<% if session[:student_id]==@student.id %>
  <%= link_to 'Edit', edit_student_path(@student) %> |
<% end %>

<%= link_to 'Back', students_path %>
