<h1>Sign Up</h1>
<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :given_name %>
    <%= form.text_field :given_name, id: :student_given_name %>
  </div>

  <div class="field">
    <%= form.label :family_name %>
    <%= form.text_field :family_name, id: :student_family_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :student_email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, id: :student_password %>
  </div>

  <div class="field">
    <%= form.label :password_confirmation %>
    <%= form.password_field :password_confirmation, id: :student_password_confirmation %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%#= form.text_field :team_id, id: :student_team_id %>
    <%= form.select :team_id, Team.all.collect{|s|[s.name, s.id]} %>
  </div>

  <div class="field">
    <%= form.label :team_password %>
    <%= form.password_field :team_password, id: :student_team_password %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.file_field :photo, id: :student_photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
