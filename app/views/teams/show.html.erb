<p id="notice"><%= notice %></p>

<%= render partial: "perfil", locals: {team: @team, edit: false} %>

<% if @student.admin %>
  <%= link_to 'Edit', edit_team_path(@team) %> |
<% end %>
<%= link_to 'Back', teams_path %>
