<p id="notice"><%= notice %></p>

<%= render partial: "perfil", locals: {team: @team, edit: false, adm: current_student.admin && current_student.team_id == @team.id} %>

<% if @student.admin && @student.team_id.to_i == @team.id %>
  <%= link_to 'Add New Tool', new_tool_path %> 
  <%= link_to 'Edit', edit_team_path(@team) %> 
<% end %>
<%= link_to 'Back', teams_path %>
