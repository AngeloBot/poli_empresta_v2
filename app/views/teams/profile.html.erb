<p id="notice"><%= notice %></p>

<%= render partial: "perfil", locals: {team: @team_profile, edit: false} %>

<% if @student.admin %>
  <%= link_to 'Edit', edit_team_path(@team_profile) %> |
<% end %>
<%= link_to 'Back', teams_path %>